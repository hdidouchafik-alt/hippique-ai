services:# Exported from Render on 2026-09-23T21:22:11Z
version: "1"
services:
- type: web
  name: hippique-ai
  runtime: python
  repo: https://github.com/hdidouchafik-alt/hippique-ai
  plan: free
  region: oregon
  buildCommand: pip install -r requirements.txt
  startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
  autoDeployTrigger: commit

  - type: web
    name: hippique-ai
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
